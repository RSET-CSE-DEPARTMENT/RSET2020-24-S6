function annInput(layer) {
   
    return {imports:["import tensorflow as tf\nfrom tensorflow import keras\nfrom tensorflow.keras.models import Sequential"],code:["model=Sequential([keras.layers.Input(shape=X_train.shape[1:]),"]}   
}

export default annInput;
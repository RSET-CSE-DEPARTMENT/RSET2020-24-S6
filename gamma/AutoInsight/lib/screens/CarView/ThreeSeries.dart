import 'package:autoinsight/screens/CarView/comp/ThreeSeriesBody.dart';
import 'package:flutter/material.dart';

class ThreeSeries extends StatelessWidget {
  const ThreeSeries({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: ThreeSeriesBody(),
    );
  }
}

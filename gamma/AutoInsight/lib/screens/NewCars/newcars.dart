import 'package:autoinsight/screens/NewCars/comp/newcarbody.dart';
import 'package:flutter/material.dart';

class newcars extends StatelessWidget {
  const newcars({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: newcarbody(),
    );
  }
}

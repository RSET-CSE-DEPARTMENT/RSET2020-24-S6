import 'package:autoinsight/screens/NewCars/BookCars/BookRecord/comp/bookrecbody.dart';
import 'package:flutter/material.dart';

class bookrec extends StatelessWidget {
  const bookrec({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: bookrecbody(),
    );
  }
}
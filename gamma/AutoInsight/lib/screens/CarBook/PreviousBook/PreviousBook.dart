import 'package:autoinsight/screens/CarBook/PreviousBook/comp/PreviousBookBody.dart';
import 'package:flutter/material.dart';

class prevbook extends StatelessWidget {
  const prevbook({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: prevbookbody(),
    );
  }
}

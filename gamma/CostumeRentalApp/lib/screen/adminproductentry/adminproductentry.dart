import 'package:corental/screen/adminproductentry/components/adminproductentrybody.dart';
import 'package:flutter/material.dart';

class AdminProductEntry extends StatelessWidget {
  const AdminProductEntry({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: AdminProductEntryBody(),);
  }
}
